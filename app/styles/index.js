import colors from './colors'
import fonts from './fonts'
import metrics from './metrics'
import app from './app'

export { colors, fonts, metrics, app }
export default { colors, fonts, metrics, app }
